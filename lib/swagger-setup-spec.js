// Generated by CoffeeScript 1.6.3
(function() {
  describe('Swagger setup', function() {
    return describe("verifies the swagger setup process", function() {
      return it("ensures success called only once", function() {
        var verify;
        window.swaggerCounter = 0;
        verify = function() {
          window.swaggerCounter = window.swaggerCounter + 1;
          return expect(window.swaggerCounter).toBe(1);
        };
        window.authorizations.add("key", new ApiKeyAuthorization("api_key", "special-key", "header"));
        window.swagger = new SwaggerApi({
          url: 'http://petstore.swagger.wordnik.com/api/api-docs',
          success: verify
        });
        return waitsFor(function() {
          return swagger.ready != null;
        });
      });
    });
  });

}).call(this);
