openapi: "3.0.4"
servers:
- url: "/v1/"
paths:
  /endpoint:
    get:
      summary: an operation!
      operationId: myOp
      parameters:
      - name: filter
        in: query
        schema:
          type: string
